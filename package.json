{
  "name": "bc-pex",
  "private": true,
  "version": "0.1.35",
  "type": "module",
  "description": "Personality Excretion (PEX) for Bondage Club",
  "scripts": {
    "dev": "vite build && concurrently -k -n build,serve \"vite build --watch\" \"vite preview --port 5174\"",
    "preview": "vite preview --port 5174",
    "build": "vite build",
    "lint": "eslint ."
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "eslint": "^9.30.1",
    "globals": "^16.3.0",
    "vite": "^7.0.4"
  }
}
